# BAN-501 Online Course Companion — Project Plan & Status Tracker

This is the **living source of truth** for porting, verifying, reviewing, and publishing the BAN-501 online course companion. Update the status markers, the Findings & Decisions Log, and the Resume Prompt at the end of every phase.

**Status markers:** `[ ]` pending · `[~]` in progress · `[x]` done · `[!]` blocked / needs user

## Status summary

| Phase | Title | Status |
|------:|-------|:------:|
| 1 | Repo scaffold & tooling | `[x]` |
| 2 | Port content + figures | `[x]` |
| 3 | Computation verification (98 numbers) | `[x]` |
| 4 | Freshness checks | `[x]` |
| 5 | Codex independent second review | `[x]` |
| 6 | Changelog | `[ ]` |
| 7 | BAN-501 cleanup + instructor guide | `[ ]` |
| 8 | Finalize & publish | `[ ]` |

---

## Context

BAN-501 ("Predictive Modeling," graduate applied-ML) is being reoffered fully online/asynchronous. The student-facing "course companion" book exists as markdown in `../BAN-501/course-companion/` (10 modules + 8 Deep Dives + a Q&A reference). A *previous* offering's online version was published as an MkDocs site in `../BAN-501-course-companion/` (that site exposed only 4 of the deep dives — it is the tooling template, not the content source). This repo ports the **current** content into a fresh MkDocs site, verifies every computed number with code, runs an independent Codex second review, adds a changelog, and retires the duplicated materials from `../BAN-501/` behind an instructor guide.

**Key source facts** (do not re-derive):
- **Content** `../BAN-501/course-companion/`: 19 md files (spaces in names) = 10 modules + 8 deep dives + `QA.md`; 23 PNG figures in `figures/{module1,module6,module7,module8,deep_dive}/`; math `$…$`/`$$…$$`; language-tagged code fences; pipe tables; **blockquote callouts** (`> **Numerical Example:** … > **Output:** … > **Interpretation:** … > *Source:*`); `%20`-encoded `.md` cross-links; **no YAML front matter**. 98 numeric examples each carry a `> *Source:*` citation.
- **Verification** `../BAN-501/course-companion-computations/`: 17 `*_examples.py` (98 `demo_*` functions) + `generate_figures.py` (marimo, writes the 23 PNGs). Verify = run function → diff stdout vs book `Output:` block. Seeds mostly `42` (one `123`; one `range(50)` loop). Some demos are NOT in `__main__` — call individually.
- **Master index** `../BAN-501/CODE_EXAMPLE_MAPPING.md`: 98 functions → file/section/line (line numbers predate May-2026 edits → drifted; regenerate after port).
- **Outline/quizzes** `../BAN-501/`: companion aligned to the 10-module outline; 12 quizzes (163 items) align 1:1. **Quiz answer letters are the deterministic output of `rebalance_answers.py` — do NOT re-run or hand-edit answers except to fix a genuine error, and note any such fix.** Prior audits report all substantive issues fixed as of ~2026-05-07/08 except Q-05b (Blackboard rendering, deferred) + watch items (deep-dive video coverage; residual pandas idioms in Modules 1/2/9; mapping line drift).
- **Env**: pixi 0.72.0; linux-64; RTX A2000 GPU, CUDA 13.0; BAN-501 heavy env already materialized at `../BAN-501/.pixi/envs/default`. Both existing `pixi.toml`s use the modern `[workspace]` table. The pixi platform nuance = feature-level `platforms` scoping + mismatched lockfile schema (6 vs 7) — validated empirically here (see Findings Log).

## Confirmed decisions (2026-07-21)

1. Site includes **all 8 Deep Dives** as appendices.
2. **Move** the computation suite (`*_examples.py`, `generate_figures.py`, `CODE_EXAMPLE_MAPPING.md`) into this repo; two-environment pixi (`docs` default + `compute` feature, feature-scoped to linux-64).
3. **Upgrade** blockquote callouts to **MkDocs Material admonitions**.
4. After full verification (+ final explicit confirmation), **remove** `course-companion/`, `course-companion-computations/`, and companion-specific audit/mapping docs from `../BAN-501/`; add an **instructor user guide** there.
5. **(2026-07-21, Phase 2)** Callout upgrade beyond the enumerated Numerical-Example/Status mapping = **"convert labeled callouts only"**: `> **Note[…]:**`→`note`, `> **Sidebar: …**`→`info`, the test-set `> **Never let…!**`→`warning`, the `**Theorem** (…)` statement→`quote`. Pure prose analogies/intuitions, the epigraph, the QA cross-ref aside, and the "Three-Component Framework" table (which appears un-boxed elsewhere) stay as blockquotes.

## Working conventions (every phase)

- Update **this file** (status, Findings Log, Resume Prompt) at each phase boundary.
- Preserve pedagogy: *Decision Model / Quality Measure / Update Method* framing; notation (`n` obs, `d` features, `C` classes, `K` clusters, `α` LR, `λ` reg — never `p` for features); `random_state=42`; **polars not pandas** (except statsmodels via `.to_pandas()`). Code readable for Master's students with one semester of Python.
- Accuracy & interpretability first; no change merely for change's sake; every load-bearing number traces to code.
- Sub-agents may fan out mechanical work, but **verify every sub-agent + Codex finding** before acting.
- Commit at phase boundaries **only after asking the user**. Commit trailer: `Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>`.
- "Done" requires: `mkdocs build --strict` clean; figures read back as images; computation outputs diffed; concrete numbers quoted.

---

## Phase 1 — Repo scaffold & tooling  `[x]`  (done 2026-07-21)

- [x] `PROJECT_PLAN.md` created (this file)
- [x] `pixi.toml` — `[workspace]` (4 platforms) + `docs` (default) & `compute` (linux-64, CUDA 13) features/environments
- [x] `pixi lock` / install both envs; pixi 0.72 platform findings recorded (Findings Log)
- [x] `mkdocs.yml` — Material, MathJax, extensions; nav = Home / 10 Modules / 8 Deep Dives / Q&A / Changelog
- [x] scaffold: `docs/index.md`, `docs/javascripts/mathjax.js`, `docs/stylesheets/extra.css`
- [x] `.github/workflows/deploy.yml` (Actions/Pages, `mkdocs build --strict`)
- [x] `.gitignore`, `README.md`
- [x] placeholder stubs for all nav pages
- [x] `mkdocs build --strict` passes on skeleton (exit 0; 21 pages; built in 0.20s)

**Verify:** `pixi run build` (strict) exit 0; both envs solve; pixi warnings recorded below.

## Phase 2 — Port content + figures  `[x]`  (done 2026-07-21)

1. Copy 23 figures → `docs/assets/{module1,module6,module7,module8,deep_dive}/`.
2. Port + rename 19 files → `docs/modules/NN-slug.md` (10), `docs/appendices/<slug>.md` (8), `docs/reference/qa.md` (1). Slugs already fixed in nav.
3. Transforms (script mechanical parts; fan out per file, verify each):
   - `figures/<sub>/x.png` → `../assets/<sub>/x.png`.
   - Blank lines around every `$$…$$`.
   - `%20`-encoded `.md` cross-links → correct MkDocs relative paths/slugs.
   - **Callout → admonition:** 5-part numerical example → single `!!! example "Numerical Example: <title>"` (4-space-indented python fence, `**Output:**` + plain fence, `**Interpretation:**`, `*Source: \`computations/<file>.py\` — \`fn()\`*`); deep-dive `> **Status:**` → `!!! note "Supplemental reading"`; `> **Theorem**` → `!!! quote "Theorem"`; "Reading the diagram" → `!!! info "Reading the diagram"`.
   - Wording: "this PDF" → "this companion/site"; keep "hands-on".
4. `mkdocs build --strict` clean.
5. Read rendered pages (module + deep-dive sample, one heavy-math, one heavy-code) — math/code/admonitions/tables/figures correct.

**Verify:** strict build clean; no `%20` links or `figures/` paths remain; visual spot-check.

## Phase 3 — Computation verification (accuracy-critical)  `[x]`  (done 2026-07-21)

1. Move `course-companion-computations/*` → `computations/`; `CODE_EXAMPLE_MAPPING.md` → repo root. Update `generate_figures.py` `OUTPUT_DIR` → `docs/assets/`. (Copy, not delete from BAN-501, until Phase 7.)
2. `computations/verify_all.py`: import each `*_examples.py`, call all 98 `demo_*` (incl. those not in `__main__`), capture stdout, compare to book numbers. Run under `pixi run -e compute` (seed 42, GPU parity).
3. Verification report (pass/fail per number). Mismatch → investigate: code right → update book number + **propagate**; code wrong → fix code. Log every change.
4. Regenerate 23 figures; **read each back as image** (figure rubric); confirm parity/improvement.
5. Refresh `CODE_EXAMPLE_MAPPING.md` (docs slugs + current line numbers).

**Verify:** every numeric claim matches code output (quote count + deltas); figures reviewed as images.

## Phase 4 — Freshness checks  `[x]`  (done 2026-07-21)

1. Library currency (sklearn ≥1.8, torch ≥2.10/torchvision ≥0.26, transformers ≥5.2, seaborn ≥0.13, polars ≥1.38, numpy ≥2.4, xgboost ≥3.2). Cross-check prior audits — don't reintroduce fixed items (`sparse_output`, `weights=…`, `penalty=None`→`C=np.inf`, XGBoost `early_stopping_rounds`).
2. Outline alignment (section-by-section); confirm all 8 deep-dive cross-refs exist in parent modules.
3. Quizzes: note-only check (do NOT modify; answer letters are `rebalance_answers.py` output). Escalate genuine errors to user.
4. pixi platform concern: document this repo's setup + empirical 0.72 behavior; report (don't silently fix) needed changes to `../BAN-501/{pixi.toml,notebooks/pixi.toml}`.
5. Residual pandas idioms scan (Modules 1/2/9).
6. `mkdocs build --strict` (dead links); "site vs PDF" wording sweep.

**Verify:** re-run touched computations; strict rebuild; no fixed-issue regressions.

## Phase 5 — Codex independent second review  `[x]`  (done 2026-07-21)

1. Codex CLI/plugin reviews in batches: (a) content accuracy/clarity, (b) computation correctness, (c) freshness findings, (d) port fidelity.
2. Reconcile Claude vs Codex per disagreement; verify against code/source; iterate to agreement. Log all.
3. Apply agreed fixes (propagate) + re-verify.

**Verify:** no open disagreements; strict build + affected computations clean.

## Phase 6 — Changelog  `[ ]`

1. `CHANGELOG.md` (Keep a Changelog, semver) at root; seed `1.0.0 — Online edition` entry.
2. Surface as `docs/changelog.md` (snippet include or thin copy) + nav.
3. Document update workflow (edit `docs/`, `pixi run -e compute verify`, changelog entry, push → auto-deploy) — feeds Phase 7 guide.

**Verify:** changelog renders in built site.

## Phase 7 — BAN-501 cleanup + instructor user guide  `[ ]`

**Touches a different repo — explicit final confirmation before any deletion.**
1. Pre-flight: confirm this repo fully supersedes source (ported + verified, computations moved + passing, figures regenerated). **Ask user to confirm.**
2. Remove from `../BAN-501/`: `course-companion/`, `course-companion-computations/`, companion-specific docs (`COURSE_COMPANION_AUDIT.md`, `CODE_EXAMPLE_MAPPING.md`, `build-companion` task in `pixi.toml`). **Keep** broader docs (`course-outline-content-audit.md`, `module-quizzes/*`) — trim only companion-specific sections.
3. Add instructor guide `../BAN-501/course-companion-GUIDE.md` (what/where the online companion lives, how to update it, how to rebuild figures). Update `../BAN-501/CLAUDE.md` references to removed paths.

**Verify:** no dangling references in `../BAN-501/` to deleted paths; guide accurate.

## Phase 8 — Finalize & publish  `[ ]`

1. Final `mkdocs build --strict` + full nav read-through; confirm 98 numbers, figures, links.
2. Verify `deploy.yml`; note required Pages "Source: GitHub Actions" setting.
3. Commit + push (ask first); confirm Actions build + Pages deploy.
4. Final status + close-out entry here.

**Verify:** live site loads; spot-checked module renders math/code/figures/admonitions.

---

## Findings & Decisions Log

*(Append dated entries as work proceeds. Record pixi platform findings, computation verification results, freshness findings, Codex reconciliation, and any content changes with rationale.)*

- **2026-07-21 — Phase 1 started.** Scaffold created (pixi two-env, mkdocs.yml full nav, docs skeleton, CI, stubs).

- **2026-07-21 — pixi platform concern (RESOLVED / documented).** This is the "recent pixi change" the user flagged. Under **pixi 0.72.0**:
  - Both existing BAN-501 tomls and this repo already use the modern `[workspace]` table (not the deprecated `[project]`), so no `[project]`→`[workspace]` migration is needed.
  - `pixi lock` emits: *"the `[system-requirements]` table is deprecated in favor of virtual packages on `platforms`"*, suggesting `platforms = [{ platform = "linux-64", cuda = "13.0" }]`.
  - **But that inline table-array syntax is NOT accepted by pixi 0.72.0** — it errors with `expected a string, found table`. So the deprecation is *announced ahead of* the replacement being landable.
  - **Action taken:** kept the working `[feature.compute] platforms = ["linux-64"]` + `[feature.compute.system-requirements] cuda = "13.0"` form (functions correctly; warning only). Documented in `pixi.toml` and `README.md`.
  - **For Phase 4 → user:** the BAN-501 root `pixi.toml` (`[system-requirements] cuda="13.0"`) and `notebooks/pixi.toml` (`[feature.gpu.system-requirements] cuda="13.0"`) emit the same deprecation warning but **should NOT be migrated yet** (the new syntax errors in 0.72.0). Also note `notebooks/pixi.lock` is schema v7 vs root `pixi.lock` v6 — a re-lock would align them; not urgent.
  - Both envs solved cleanly: **compute** → python 3.14.6, pytorch-gpu 2.11.0 (cuda129), torchvision 0.26.0, transformers 5.14.1, scikit-learn 1.9.0, numpy 2.4.6, xgboost 3.3.0, polars 1.42.1, scipy 1.18.0, marimo 0.20.4, shap 0.51.0, statsmodels 0.14.6. **docs** → mkdocs 1.6.1, mkdocs-material 9.7.7 (python 3.14.6), all 4 platforms.
  - **Phase-3 note:** the compute env resolved some libs NEWER than what produced the book numbers (sklearn 1.9 vs ≥1.8; transformers 5.14 vs ≥5.2; pytorch 2.11). Minor low-order-digit drift is possible; treat any drift beyond the book's reported precision as a freshness finding (update + propagate).

- **2026-07-21 — MkDocs 2.0 advisory (FYI, no action).** mkdocs-material 9.7.7 prints a banner warning that a future MkDocs 2.0 will be backward-incompatible (plugins/theming). Informational only; does not affect this build.

- **2026-07-21 — Phase 1 complete.** `pixi run build` (`mkdocs build --strict`) exits 0, generates 21 pages (home + 10 modules + 8 deep dives + qa + changelog) in ~0.2s. Skeleton is buildable with the full intended nav; module/appendix/qa pages are placeholder stubs to be filled in Phase 2, changelog in Phase 6.
  - Committed to `main` as `8cbe679` (initial commit). Not pushed — this local mirror shows `origin/main [gone]`; the GitHub remote may need to be (re)created before the Phase 8 deploy.

- **2026-07-21 — Phase 2 complete (port content + figures).** All 19 source md files ported into `docs/` (10 → `modules/NN-slug.md`, 8 → `appendices/<slug>.md`, QA → `reference/qa.md`) and 23 figures copied → `docs/assets/{module1,module6,module7,module8,deep_dive}/`. Executed via two deterministic, content-preserving scripts (in scratchpad) rather than generative rewriting, to guarantee the 98 numeric outputs survive byte-for-byte.
  - **Transforms applied (counts):** 23 image paths `figures/…`→`../assets/…`; 8 `%20`-encoded `.md` cross-links → correct relative slugs (all module→appendix, e.g. `../appendices/imbalanced-data.md`); 111 single-line `$$…$$` display equations blank-line-normalized — **all display math is single-line (no lone `$$`)**, and consecutive runs (e.g. the 6 LSTM gate equations in Module 8) get exactly one blank inserted between neighbors, not collapsed; source citations `course-companion-computations/`→`computations/` and ` - `→` — ` (em-dash) per the mapping.
  - **Callouts:** 98 five-part Numerical Example blockquotes → `!!! example "Numerical Example: …"` (4-space-indented python fence, blank injected before the `**Output:**` fence, `*Source: \`computations/…\` — \`fn()\`*`). 8 deep-dive `> **Status:**` → `!!! note "Supplemental reading"`. Plan's `> **Theorem**` / "Reading the diagram" mappings were **moot** — neither pattern exists in the corpus. The "this PDF"→site wording transform found **nothing to change** (no PDF/print references exist; QA's generic "This document" is web-appropriate, left as-is).
  - **Labeled-blockquote sub-decision (user, this session):** "convert labeled callouts only" → 7 additional conversions: 4 `Note[…]`→`note`, 1 `Sidebar: Batch Normalization`→`info`, 1 test-set `Warning`, 1 `Theorem (Cybenko…)`→`quote`. **12** blockquotes intentionally left (6 cnn-architecture prose asides, the data-prep epigraph, 3 "Intuition/Analogy" blocks, the "Three-Component Framework: CNNs" table, the QA cross-ref aside). See Confirmed decisions #5.
  - **Fidelity verification (deterministic):** a checker compares every Numerical-Example fence's de-quoted source content vs de-indented dest content → **ZERO byte differences across all 98 examples** (the numbers Phase 3 verifies are intact). Whole-file content-signature diff = exactly the 7 folded bold-label lines (moved into admonition titles), no other content missing/added on either side. All 7 converted bodies confirmed present verbatim.
  - **Build & negative checks:** `mkdocs build --strict` exit 0, 21 pages, ~1.2s, no link/nav warnings. Zero stray `figures/`, `%20`, `course-companion-computations/`, or raw `> **Numerical Example`/`> **Status:` remain in `docs/`. Final rendered-HTML admonition inventory: **example=98, note=12 (8 status + 4 Note), info=1, warning=1, quote=1**.
  - **Rendered spot-checks (headless google-chrome-stable, served build):** Module 3 (math + example admonition + tables), Module 8 (22 display eqs incl. LSTM run rendering as separate blocks; TF-IDF/Word2Vec math; syntax-highlighted code), Module 9 (heavy code), universal-approximators (Supplemental-reading `note`, `quote` Theorem with `ℝⁿ`, multi-panel figures from `../../assets/deep_dive/`), data-preparation (`note` + epigraph-as-blockquote + polars code + table), Module 7 (`info` Sidebar), data-prep (`warning`). All math (MathJax), code (pygments), admonitions, tables, and figures render correctly. (Screenshots in scratchpad; not committed.)
  - **Not committed** (per "commit only after asking"). Working tree now has 19 populated `docs/` pages + 23 assets + the modified `PROJECT_PLAN.md`.

- **2026-07-21 — Phase 3 complete (computation verification, 98 numbers).** Copied the suite into `computations/` (17 `*_examples.py` + `generate_figures.py`) and `CODE_EXAMPLE_MAPPING.md` to repo root (source still in `../BAN-501/`, deleted in Phase 7); set `generate_figures.py` `OUTPUT_DIR` → `docs/assets`. Wrote `computations/verify_all.py` (`pixi run -e compute verify`): imports each file, runs all 98 `demo_*` in definition order (matching each file's `__main__`; `deep_dive_surprising_phenomena` has none, so its 7 are called individually), captures stdout, and does a **numeric-aware** comparison against the ported docs (the book `Output:` blocks are curated transcriptions, not byte-identical to stdout, so a text diff is meaningless).
  - **Final result: 98/98 clean — 81 direct matches + 17 adjudicated, 0 unexplained flags, 0 errors.** All 98 functions execute on the pinned compute env (torch 2.11 GPU, sklearn 1.9, numpy 2.4). The 17 adjudicated residuals are prose-derived arithmetic / "~" approximations / sign conventions / rounded presentation tables / reference constants — each traced by hand and encoded in `ADJUDICATED` in `verify_all.py` with a reason; the checker reports any *new* unmatched token as UNEXPLAINED, so the allowlist cannot mask a future regression.
  - **Code fixes (code wrong → fixed code):**
    - `module8_examples.py::demo_vanishing_gradient` — the "reduced to 0.000027%" line was mislabeled by 100× (0.9¹⁰⁰ = 0.0000265614 is **0.0027%** of the original gradient, not 0.000027%). The book was already correct at 0.0027%. Fixed the code to compute `{0.9**100*100:.4f}%`.
    - `deep_dive_surprising_phenomena_examples.py::demo_bias_variance_tradeoff` — **freshness/numerical-stability**: the raw `LinearRegression` on an unscaled Vandermonde matrix drifted at high degree under sklearn 1.9/numpy 2.4 (deg 12 test 0.3458→0.3308; deg 15 broke down — train MSE jumped *up* to 0.4602, contradicting the "train error keeps dropping" narrative). Per your decision ("stabilize the demo"), wrapped the fit in `make_pipeline(PolynomialFeatures, StandardScaler, LinearRegression)`. This **exactly restores the book's original numbers** (deg 12 → 0.1267/0.3458, deg 15 → 0.1249/0.3610) with monotone train MSE, and is reproducible across library versions. Book Output unchanged; updated its displayed snippet to show the scaled pipeline.
    - `deep_dive_surprising_phenomena_examples.py::demo_double_descent_random_features` — the shipped function had been rewritten to a Fourier basis whose output no longer reproduced the book (peak 2.751, weak second descent), and the book's *own displayed snippet* (random features, α=1e-8) blows up to test MSE >120,000 on the pinned env — neither reproduced the book's clean numbers. Per your decision ("rewrite demo to work"), replaced it with the canonical, well-conditioned Hastie et al. min-norm least-squares setup (isotropic Gaussian, signal in 140/900 dims, averaged over 40 draws, straddling p=n). Reproducible; cleanly shows the interpolation peak (39.1 at p/n≈1) and a second descent (2.34 at p/n=1.75) that beats the sweet spot (5.20). Synced the book Output + snippet + interpretation + admonition title. (Function keeps its `_random_features` name to preserve the Source citation; docstring/title clarify it is now min-norm regression — a candidate rename, deferred to avoid churn.)
  - **Book fixes (code right → updated book):**
    - `module2::demo_gradient_descent_convergence` — the iter-10 row β₀/β₁ (1.0889/2.2613) were a **transcription error**: those betas give MSE 3.2341, not the 3.7994 shown in the same row; all other iterations matched exactly. Corrected to the code's 0.6787/2.3374.
    - `demo_emergence_metric_mirage` — deterministic sigmoid math; book values were stale (11.2→11.9, 9.2→9.3, 20.8→20.9, 52.8→53.0, 78.3→78.4; scale-13 Appearance "Gradual vs jump"→"Both high"). Now reproduce exactly.
    - `demo_grokking_simulation` — explicitly illustrative hand-simulation; per your decision ("update book to current"), replaced the Output table with the current reproducible trajectory (later epochs 10.0/35.0/70.0/82.5/95.0%; phases at 25k/30k → "Grokking!"/"Almost there").
    - `deep_dive_universal_approx::demo_logistic_regression_equiv` — freshness drift in the (inherently unstable) sklearn coefficients on perfectly-separable data (108.4445→108.4925, 216.0017→216.1004, −53.9592→−53.9849); PyTorch coeffs, ratio, and accuracy unchanged.
    - `module8::demo_bert_vs_gpt_scale` — book Output "~1591x" (from its simplified snippet) → "~1600x" to match the cited function (and the book prose, which already said ~1,600x).
    - `module4::demo_rf_vs_single_tree` — "22% lower variance" was actually the *std* reduction; corrected to "22% lower standard deviation (a 40% lower variance)" — the code itself prints "40% lower variance".
    - `module9::demo_shap_to_business` — illustrative action text "within 48 hours" → "within 24 hours" to match the cited function's output.
  - **Documented convention (defensible-but-undocumented):** `deep_dive_timeseries::demo_time_series_decomposition` presents its Observed/Residual columns **rounded to the nearest $1,000** (46,000 vs code 45,993, etc.) for teaching clarity; the exact trend/seasonal structure is preserved. Recorded in the `ADJUDICATED` reason.
  - **Figures:** regenerated all 23 into `docs/assets/` from the relocated `generate_figures.py` (0 errors) and reviewed every one back as an image against the figure rubric — labels/units/titles self-explanatory, ticks legible, colorblind-reasonable palette, chart types fit. The four *Surprising Phenomena* figures (double_descent, grokking, bias_variance_u_curve, emergent_abilities) are **idealized analytic schematics in `generate_figures.py`, decoupled from the demo functions** (verified), so they carry no tabulated numbers and stay consistent with the updated example narratives.
  - **Mapping & build:** regenerated `CODE_EXAMPLE_MAPPING.md` from the docs (98 functions → docs page + admonition title + current line, 0 missing). `mkdocs build --strict` exits 0 (~1.3s; only the informational mkdocs-material 2.0 banner). Added `computations/_verify/` (raw outputs, report, logs) to `.gitignore` — regenerable via `pixi run -e compute verify`.
  - **Not committed** (per "commit only after asking").

- **2026-07-21 — Phase 4 complete (freshness checks).** Started from the committed Phase 3 state (`fe6c802`); baseline `pixi run -e compute verify` re-confirmed **98/98 clean** (81 PASS + 17 ADJUDICATED, 0 unexplained, 0 errors) before any edits.
  - **(1) Library currency — CLEAN.** Resolved compute-env versions all meet/exceed the floors: scikit-learn **1.9.0**, torch **2.11.0**, torchvision **0.26.0**, transformers **5.14.1**, seaborn **0.13.2**, polars **1.42.1**, numpy **2.4.6**, xgboost **3.3.0**, scipy 1.18.0, statsmodels 0.14.6, shap 0.51.0, umap-learn 0.5.12, gensim 4.4.0, matplotlib 3.11.1. **No reintroduced deprecations:** `C=np.inf` present (with the sklearn-1.8 comment); `make_classification(weights=[…])` is the *current* API, not the deprecated one; XGBoost is never imported (`early_stopping_rounds` appears only inside a `print` string, not a `.fit()` call); no `OneHotEncoder(sparse=…)`/`sparse_output`, no removed numpy aliases (`np.int`/`np.float`/`np.trapz`/`np.in1d`/…), no seaborn `distplot`, no torch `Variable`/`get_cmap`. **Empirical:** ran all 98 demos under `warnings.simplefilter("always")` → **0 warnings of any category**; the harness was self-tested (it captured both a synthetic `DeprecationWarning` and a real sklearn `ConvergenceWarning`), so the zero is genuine, not a suppressed filter.
  - **(2) Outline alignment — CLEAN.** The 10 `docs/modules/` pages match `../BAN-501/course-outlines/course-outline.toc` 1:1 (titles + order). All 8 deep dives are cross-referenced from their parent modules, and the parent→DD mapping matches the source exactly (M1→data-prep + model-eval, M3→imbalanced, M6→universal-approx + surprising-phenomena, M7→CNN, M8→transformer, M10→time-series), cross-checked against the source's own `module-quizzes/optional-practice-deep-dives.md` source policy.
  - **(3) Quizzes — note-only, no changes.** Quizzes live in `../BAN-501/module-quizzes/` (12 quizzes / 163 items) and are **not part of this companion site** (nav = Home/Modules/Deep Dives/QA/Changelog), so nothing to modify here. Answer letters remain `rebalance_answers.py` output. Nothing genuine surfaced to escalate; prior-audit status stands (all substantive issues fixed ~2026-05-07/08 except Q-05b Blackboard rendering, deferred).
  - **(4) pixi platform — documented; report for `../BAN-501` (do NOT fix).** Empirically re-confirmed on pixi 0.72.0: BAN-501 root `pixi.toml` (`[workspace]` + `[system-requirements] cuda="13.0"`, L8-9) and `notebooks/pixi.toml` (`[workspace]` + `[feature.gpu.system-requirements] cuda="13.0"`, L11-12) both use the modern `[workspace]` table (no `[project]` migration needed) but emit the `[system-requirements]`-deprecation warning. The suggested replacement `platforms = [{ platform = "linux-64", cuda = "13.0" }]` **still errors** ("expected a string, found table") → keep `[system-requirements]` in both until a pixi release lands the inline form. **Lockfile schema drift (confirmed):** root `pixi.lock` = **v6**, `notebooks/pixi.lock` = **v7**, this repo = **v7**; a `pixi lock` on the BAN-501 root would regenerate it as v7 and align them (not urgent — v6 still solves).
  - **(5) Residual pandas idioms (Modules 1/2/9) — 5 pandas-only accessors fixed (illustrative snippets, not verified-98).** All fixes are in top-level ```` ```python ```` teaching fences **outside** any `!!! example` admonition, so the 98 verified numbers are untouched. `.iloc`/`.values` don't exist on polars frames (would `AttributeError` a polars-first student). Converted to `.to_numpy()` (polars-and-pandas-safe):
    - Module 9 (SHAP/LIME): `X_test.iloc[0]` ×2 → `X_test.to_numpy()[0]`; `X_train.values` → `X_train.to_numpy()`; `X_test.iloc[0].values` → `X_test.to_numpy()[0]`. Added `feature_names=feature_names` to `shap.force_plot` (the pandas Series index had supplied labels the bare numpy row lacks — behavior-preserving).
    - Module 2 (VIF snippet): `variance_inflation_factor(X.values, i)` → `X.to_numpy()` (this one was initially under-reported — my first grep pattern omitted `.values`; it is a *separate* illustrative fence, not the sanctioned `smf.ols` block).
    - Module 1 (cosmetic): `sns.histplot(df['column'], kde=True)` → `…df['column'].to_list()…` for consistency with the module's other two histplot calls.
    - **Sanctioned exception left in place:** Module 2's `import pandas as pd` + `pd.DataFrame` under the `smf.ols` example — already annotated with `!!! note "Note on pandas in a polars-first course"` (statsmodels' formula API needs pandas-style frames). This is the *only* remaining pandas in `docs/` and is intentional. Both approved by user (2026-07-21).
  - **(6) Strict build + PDF wording — CLEAN.** `mkdocs build --strict` exits 0 post-edit, no broken-link/nav warnings (only the two documented informational banners: pixi `[system-requirements]` + MkDocs 2.0). "Site vs PDF" sweep: no print-artifact references remain; "This companion"/"This document" framing is web-appropriate.
  - **Watch item — demo rename: recommend KEEP.** `demo_double_descent_random_features` retains its name. The docstring ("Demonstrate double descent with min-norm linear regression") and the book admonition title ("Double Descent in Min-Norm Regression") already name the estimator; "random features" stays accurate (the design matrix *is* random Gaussian features, p swept). A rename would touch 3 coupled sites (the `*Source:*` line, `CODE_EXAMPLE_MAPPING.md`, the verify harness's Source key) for no clarity gain — not worth the churn.
  - **Verify (post-edit):** `pixi run -e compute verify` re-run after the doc edits stays **98/98 clean** (edits are outside all verified admonitions, as expected). `mkdocs build --strict` clean. No fixed-issue regressions.
  - **Not committed** (per "commit only after asking"). Working tree: `docs/modules/{01,02,09}` (5 pandas-idiom fixes) + this `PROJECT_PLAN.md`. **(Committed after user approval as `481dba5`.)**

- **2026-07-21 — Phase 5 complete (Codex independent second review).** Ran Codex (codex-cli 0.144.5) via the `codex:codex-rescue` plugin in two read-only batches; **verified every Codex finding against the code/source before acting** (Codex was accurate but not infallible — see disagreements). Started from committed `481dba5`; baseline verify re-confirmed 97/98.
  - **Batch B — computation correctness (Codex job `task-mrusf5ol`).** Codex re-ran `verify_all.py` and stress-tested the "clean" claim. **5 findings, all verified genuine; reconciled + fixed:**
    - **`demo_permutation_importance` (module9) — real content error.** Code generated `x1,x2,x3` independently (realized r≈0) yet the book explained x2's negative importance as "correlated with x1 (r≈0.96), redundant proxy." Numbers matched code; the *explanation* was false. **Fixed (user: "real correlation"):** x2 now a genuine 0.95-correlated proxy (`x2 = 0.95·x1 + √(1−0.95²)·noise`), target on x1 only, shuffle averaged over 30 repeats. New reproducible result (baseline 73.7%; x1 +17.9%, x2 −0.4%, x3 −1.5%) genuinely demonstrates the redundancy trap; book synced; prints realized r=0.95. Sibling `demo_shap_vs_permutation` checked — it already generates real r≈0.96 (`x2 = x1 + 0.3·noise`), no change.
    - **`demo_bias_variance_tradeoff` (surprising-phenomena) — interpretation contradicted its table.** Table showed degree 5 (0.1808) < degree 2 (0.1968) but text claimed "degree 2 lowest." Root cause: 15-point test set + a latent original error (deg 2/3 are mathematically near-tied). **Fixed (user: "make deg 2 truly lowest"):** enlarged to `n_samples=500, test_size=0.7` (150 train / 350 test) → degree 2 now the strict minimum (0.2658) with a clean monotone U; aligned the displayed snippet to the function's `train_test_split`+`include_bias=False` so it reproduces the table (Codex finding 4).
    - **`_match_one` tolerance too loose + vacuous PASS (Codex 2, 5) — hardened `verify_all.py`.** Removed the ±1-last-digit slack → exact match at displayed precision; added a NO-NUMS category for the one qualitative demo (`demo_shap_summary_interpretation`). This surfaced 7 prose/formula/index tokens previously masked by the slack (`84000`, `50`, `71`/`91`, grokking `1`, positional `7`, backprop `−1`, shap `2`) — each independently verified as correct-but-not-in-stdout and added to `ADJUDICATED`. Removed the now-obsolete permutation `0.96` adjudication (correlation is real now). Honest result: **97/98 numerically verified (78 exact PASS + 19 documented adjudications) + 1 qualitative, 0 FLAG, 0 ERROR** — a stronger guarantee than the old ±1-slack "98/98." Codex re-confirmed the other 16 adjudications and the two other Phase 3 changes (vanishing-gradient 0.0027%, double-descent 12 MSEs).
  - **Batch A/C/D — content accuracy + port fidelity + freshness (Codex job `task-mruttyyl`).** **Port fidelity CLEAN** (98 admonitions, 0 malformed, links/math/images resolve, `QA.md` byte-identical, strict build clean). **My two Phase 5 demos independently reproduced by Codex.** Codex flagged **1 blocker + ~17 content issues in the ported prose** — all pre-exist in the professor's `../BAN-501` source (port was faithful). Verified each; user chose **"fix all genuine."**
    - **Left as-is (verified defensible / Codex overreach):** "logistic regression is a regression model at its core" (correct GLM framing); CNN "same input→output mapping" (counts + conclusions right, phrasing loose); BERT/GPT ratio (already adjudicated "illustrative"); multicollinearity "ignore for prediction" (defensible in-distribution); cross-entropy "exponentially worse" (minor).
    - **Fixed — 14 clear-cut (prose + demos):** fairness AUC→recall conflation; churn TP/FP internal inconsistency (L638 vs L643); CV "mean±std" mislabeled a confidence interval; WLS wrongly recommended for *directional* asymmetric costs; ICE wrongly said to fix PDP's correlated-feature (off-manifold) problem; DBSCAN "handles different densities" + "border → nearest core point" (both wrong); self-attention "permutation-invariant" → equivariant; LayerNorm snippet `x.std()` (sample) → `correction=0` (population) to reproduce the shown 11.2; Module 3 missing-value self-contradiction; **`demo_relu_bump_construction`** clipped-ramp mislabeled a "bump" → rewrote to a true triangular bump `ReLU(x)−2·ReLU(x−1)+ReLU(x−2)` (demo + book + regenerated `relu_bump.png`, reviewed); **`demo_interpolation_threshold_peak`** hardcoded "PEAK"/"Descending" labels → data-driven labels (peak on the actual max p=48; no false "descending" on the p=80 rise); dangling "Section 9.4" ref; "embeddings in Module 6" → 8; arithmetic ($27K not $18K time-series swing; 50-year age = 50 units; $2.50 revenue ≠ 250% ROI).
    - **Fixed — 3 pedagogical redesigns (user-approved depth):** **Fairness impossibility (1.1, blocker) →** rewrote the section + `demo_impossibility_theorem` to the *correct* Chouldechova/Kleinberg result (equalized odds + predictive parity can't coexist under unequal base rates): equal TPR/FPR forces precision 85.7% vs 63.2%; equal precision+TPR forces FPR 40.0% vs 11.4%. **PCA vs t-SNE (1.5) →** the demo ran PCA(2) on 2-D `make_moons` (a rotation, not a projection); rewrote to lift the moons into 50-D nonlinearly (PCA 0.333 overlap vs t-SNE 0.629) and use 50-D blobs (PCA 0.879 recovers), so PCA genuinely projects. **CLIP loss (1.7) →** replaced the single-direction equation with the symmetric image↔text average.
  - **Verify / build (post-all-fixes):** `pixi run -e compute verify` → **97/98 verified + 1 qualitative, 0 FLAG, 0 ERROR, clean=True**; all changed demos PASS (impossibility 19, pca_vs_tsne 6, permutation 12, bias-variance 26, interpolation 35, relu_bump 43, layer_norm 26). `mkdocs build --strict` exit 0. `relu_bump.png` regenerated + reviewed as an image. `CODE_EXAMPLE_MAPPING.md` regenerated (98 fns, 0 missing). No open Claude-vs-Codex disagreements remain.
  - **Not committed** (per "commit only after asking"). Working tree: 7 computation files + 13 `docs/` pages + `relu_bump.png` + `CODE_EXAMPLE_MAPPING.md` + this `PROJECT_PLAN.md`.

---

## Resume Prompts

Paste the block for the phase you're resuming into a fresh session.

> **Resume — Phase 3 (Computation verification — 98 numbers).** Repo: `/home/nick/Desktop/git-repos/github-backup/nkfreeman-teaching/BAN-501-online-course-companion`. Read `PROJECT_PLAN.md` first (status + Confirmed decisions + Findings Log). Phases 1–2 are complete: the MkDocs site is fully ported (19 files in `docs/`, 23 figures in `docs/assets/`) and builds `--strict` clean; all **98** numerical examples are `!!! example` admonitions whose `*Source:*` lines already cite `computations/<file>.py — demo_*()` (the not-yet-moved location) — verified byte-identical to source. Now execute Phase 3, running Python under `pixi run -e compute` (NOT the docs env): **(1)** COPY `../BAN-501/course-companion-computations/*` → `computations/` and `../BAN-501/CODE_EXAMPLE_MAPPING.md` → repo root (do **not** delete from BAN-501 until Phase 7); set `generate_figures.py` `OUTPUT_DIR` → `docs/assets/`. **(2)** Write `computations/verify_all.py` that imports each of the 17 `*_examples.py`, calls all 98 `demo_*` functions (including those **not** under `__main__` — call individually), captures stdout, and diffs against the `**Output:**` blocks in the ported `docs/` pages (seeds mostly 42; one 123; one `range(50)` loop; RTX A2000 / CUDA 13 GPU parity). **(3)** Produce a pass/fail report per number; on mismatch investigate — code right → update the book number in `docs/` **and propagate** to every dependent number/figure; code wrong → fix code; log every change in the Findings Log. **(4)** Regenerate all 23 figures and **read each back as an image** against the figure rubric. **(5)** Refresh `CODE_EXAMPLE_MAPPING.md` with the new `docs/` slugs + current line numbers. Watch item: the `compute` env resolved some libs **newer** than produced the book numbers (sklearn 1.9, transformers 5.14, pytorch 2.11) — treat drift beyond the book's reported precision as a freshness finding (update + propagate). Honor conventions (`random_state=42`, polars, **quizzes are fixed — do not edit**). Verify: every numeric claim matches code output (quote the count + any deltas); strict rebuild clean; figures reviewed as images. Then update `PROJECT_PLAN.md` status + Findings Log and write the Phase 4 resume prompt.

> **Resume — Phase 4 (Freshness checks).** Repo: `/home/nick/Desktop/git-repos/github-backup/nkfreeman-teaching/BAN-501-online-course-companion`. Read `PROJECT_PLAN.md` first (status + Confirmed decisions + **Findings Log**, especially the 2026-07-21 Phase 3 entry). Phases 1–3 are complete: site ported + builds `--strict` clean; **all 98 numeric examples verified 98/98 clean** against code on the pinned `compute` env (`pixi run -e compute verify` → 81 direct + 17 adjudicated, 0 unexplained, 0 errors; `computations/_verify/` is git-ignored/regenerable); 23 figures regenerated + reviewed; `CODE_EXAMPLE_MAPPING.md` refreshed. Phase 3 already changed code (`demo_vanishing_gradient` %-label, `demo_bias_variance_tradeoff` StandardScaler pipeline, `demo_double_descent_random_features` → min-norm rewrite) and several book Output blocks — do **not** re-litigate those. Now execute Phase 4 (run Python under `pixi run -e compute`): **(1)** Library-currency scan — confirm the code uses idioms current for the resolved versions (sklearn 1.9, torch 2.11/torchvision 0.26, transformers 5.14, seaborn 0.13+, polars 1.42, numpy 2.4, xgboost 3.3) and did **not** reintroduce already-fixed deprecations (`sparse_output`, `weights=…`, `penalty=None`→`C=np.inf`, XGBoost `early_stopping_rounds`); note any code emitting `DeprecationWarning`/`FutureWarning` when the 98 demos run. **(2)** Outline alignment section-by-section; confirm all 8 deep-dive cross-refs exist in their parent modules. **(3)** Quizzes: **note-only** — do NOT modify (answer letters are `rebalance_answers.py` output); escalate any genuine error to the user. **(4)** pixi platform concern: document this repo's setup + the empirical 0.72 behavior already in the Findings Log; **report (don't silently fix)** the changes needed in `../BAN-501/{pixi.toml,notebooks/pixi.toml}` (deprecated `[system-requirements]`; lockfile schema v6 vs v7). **(5)** Residual pandas-idiom scan in Modules 1/2/9 (polars-first, except statsmodels via `.to_pandas()`). **(6)** `mkdocs build --strict` (dead links) + a "site vs PDF" wording sweep. Watch item: `demo_double_descent_random_features` keeps its `_random_features` name after the min-norm rewrite (docstring/title clarify) — flag if you think it should be renamed. Verify: re-run any touched computations (`pixi run -e compute verify` stays 98/98 clean); strict rebuild clean; no fixed-issue regressions; quote concrete findings. Then update `PROJECT_PLAN.md` status + Findings Log and write the Phase 5 resume prompt.

> **Resume — Phase 5 (Codex independent second review).** Repo: `/home/nick/Desktop/git-repos/github-backup/nkfreeman-teaching/BAN-501-online-course-companion`. Read `PROJECT_PLAN.md` first (status + Confirmed decisions + **Findings Log**, especially the 2026-07-21 Phase 3 and Phase 4 entries). Phases 1–4 are complete: site fully ported + builds `--strict` clean; **all 98 numeric examples verified 98/98 clean** on the pinned `compute` env (`pixi run -e compute verify` → 81 PASS + 17 ADJUDICATED, 0 unexplained, 0 errors; `computations/_verify/` is git-ignored/regenerable); 23 figures regenerated + reviewed; library currency clean (sklearn 1.9 / torch 2.11 / transformers 5.14 / polars 1.42 / numpy 2.4 / xgboost 3.3 — 0 Deprecation/Future warnings across all 98 demos); outline aligns 1:1 and all 8 deep-dive cross-refs exist; 5 residual pandas-idiom accessors fixed in Modules 1/2/9 (illustrative snippets, outside the verified 98 — see Phase 4 entry item 5); pixi platform + BAN-501 lockfile-drift documented (report-only). **Do not re-litigate** the Phase 3 code/book changes or the Phase 4 pandas fixes. **Watch/decided:** `demo_double_descent_random_features` name is intentionally kept (Phase 4 recommendation — do not rename without deciding to touch all 3 coupled Source sites). If Phase 4 was just committed, note the commit hash. Now execute Phase 5 — an **independent second review by Codex** (use the `codex:rescue`/`codex:codex-rescue` plugin, batching so each pass has focused scope): **(a)** content accuracy & clarity of the ported prose, **(b)** computation correctness (spot-check the 98 demos + the `verify_all.py` numeric-match logic + the 17 `ADJUDICATED` allowlist reasons — is any adjudication masking a real mismatch?), **(c)** the Phase 4 freshness findings (library idioms, pandas fixes, pixi/lockfile report), **(d)** port fidelity (admonition structure, math, cross-links, figures). **Reconcile Claude vs Codex per disagreement — verify each against the code/source before acting; iterate to agreement; log every disagreement + resolution in the Findings Log.** Apply only agreed fixes and **propagate** (re-run `pixi run -e compute verify` to stay 98/98; regenerate any affected figure and read it back; `mkdocs build --strict` clean). Honor conventions (`random_state=42`, polars-first except the sanctioned statsmodels `smf.ols` pandas block, **quizzes are fixed — do not edit**). Verify: no open disagreements; strict build + all affected computations clean; quote concrete deltas. Then update `PROJECT_PLAN.md` status + Findings Log and write the Phase 6 resume prompt. Commit at the phase boundary **only after asking the user**.

> **Resume — Phase 6 (Changelog).** Repo: `/home/nick/Desktop/git-repos/github-backup/nkfreeman-teaching/BAN-501-online-course-companion`. Read `PROJECT_PLAN.md` first (status + Confirmed decisions + **Findings Log**, especially the 2026-07-21 Phase 5 entry). Phases 1–5 are complete and committed (Phase 5 = `<hash>` — fill in): site fully ported + builds `--strict` clean; computation suite **97/98 numerically verified + 1 qualitative, 0 FLAG, 0 ERROR** on the pinned `compute` env after the Phase 5 hardening of `verify_all.py` (exact-match tolerance + NO-NUMS; `pixi run -e compute verify`); 23 figures reviewed (incl. the regenerated `relu_bump.png`); `CODE_EXAMPLE_MAPPING.md` current; library currency clean; Codex second review reconciled (Phase 5 rewrote `demo_permutation_importance`, `demo_bias_variance_tradeoff`, `demo_impossibility_theorem` → correct Chouldechova theorem, `demo_pca_vs_tsne` → 50-D data, `demo_relu_bump_construction` → true triangular bump, `demo_interpolation_threshold_peak` → data-driven labels, plus ~12 prose corrections and the CLIP symmetric loss). **Do not re-litigate** any Phase 3/4/5 change. Now execute Phase 6: **(1)** create `CHANGELOG.md` at repo root (Keep a Changelog format, semver), seeding a `1.0.0 — Online edition` entry that summarizes the port + verification + review (draw specifics from the Findings Log). **(2)** Surface it in the site as `docs/changelog.md` (thin copy or a snippet include) and confirm it's in the `mkdocs.yml` nav. **(3)** Document the update workflow (edit `docs/`, `pixi run -e compute verify`, add a changelog entry, push → auto-deploy) — this feeds the Phase 7 instructor guide. Verify: `mkdocs build --strict` clean and the changelog renders in the built site. Then update `PROJECT_PLAN.md` status + Findings Log and write the Phase 7 resume prompt. Commit at the phase boundary **only after asking the user**.
